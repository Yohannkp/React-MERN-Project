# Configuration pour le déploiement sur GitHub
# Copiez ce fichier vers .env et remplissez vos valeurs

# Port du serveur (Codespaces utilise le port 5000 par défaut)
PORT=5000

# URI MongoDB 
# Pour MongoDB Atlas (recommandé pour la production)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority

# Ou pour MongoDB local (développement)
# MONGO_URI=mongodb://localhost:27017/leboncoin

# Clé secrète JWT (changez cette valeur en production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environnement
NODE_ENV=development

# Note: GitHub Codespaces expose automatiquement les ports
# Votre API sera accessible via l'URL générée par Codespaces
